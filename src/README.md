### Monitoring of Spring application using micrometer, prometheus and grafana.

First, we need initialize project using `spring-boot-actuator`. \
After project's initialization should have added endpoint like: \
`/health`, `/info`
